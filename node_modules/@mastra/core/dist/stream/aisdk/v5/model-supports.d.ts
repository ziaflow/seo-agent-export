type SupportLevel = 'full' | 'partial' | 'none';
interface ResponseFormatCapability {
    support: SupportLevel;
    jsonMode: boolean;
    jsonSchema: boolean;
    notes: string;
}
interface ToolCallsCapability {
    support: SupportLevel;
    structured: boolean;
    notes: string;
}
interface ModelCapabilities {
    responseFormat: ResponseFormatCapability;
    toolCalls: ToolCallsCapability;
    reasoning: boolean;
}
interface ModelSupport {
    modelId: string;
    provider: string;
    capabilities: ModelCapabilities;
}
export declare function getModelSupport(modelId: string, provider: string): ModelSupport | undefined;
export {};
//# sourceMappingURL=model-supports.d.ts.map